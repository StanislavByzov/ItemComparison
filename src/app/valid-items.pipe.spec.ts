import { ValidItemsPipe } from './valid-items.pipe';

describe('ValidItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
